# JAKH.net audit remediation ledger

**Baseline audit:** JAKH-2026-08-01-15A

**Audit baseline:** public revision `70aa602e7cc5acc97ccbbf936f497075d1c794c0`, evidence cutoff 2026-08-01 08:03:16 UTC

**Remediation workspace:** `jakh-net-audit-remediation`, branch `codex/audit-remediation`

**Ledger date:** 2026-08-01

**Scope:** every detailed finding ID in the audit report plus the `H-01`–`H-17` high-severity summary crosswalk; `I18N-01` was already classified as resolved at the audit baseline

**Production status:** **no remediation in this ledger has been deployed to `jakh.net`, `api.jakh.net`, the production D1 database, DNS, mail, GitHub Pages, or another production service.** One external, non-production Cloudflare resource was created for the recovery-drill workflow: D1 database `jakh-recovery-drill`, ID `704609ac-4adc-411b-8a9c-894da397ee43`, region EEUR. It is not bound to the production API Worker, and no completed backup, import, restore, or rollback drill is evidenced. All finding statuses otherwise describe the isolated branch, not live production.

**Live verification snapshot (2026-08-01 20:39 UTC):** `https://jakh.net/` returned `200` from the current Cloudflare/GitHub Pages path with `Cache-Control: max-age=600`, without the planned `X-JAKH-Site-Version`, CSP, or HSTS response headers. `https://api.jakh.net/api/health` returned service `jakh-api` version `1.4.0`, schema `6`. Public GitHub evidence showed `main` at `37d81b41c784ada25cf6d3e22d88f55b463398ef` and unprotected; the `production` environment had no protection rules or deployment-branch policy. The same upstream commit's API validation, Pages deployment, and added EC2 deployment all failed. The EC2 workflow and its nondeterministic builder bypassed quarantine and release receipts, so the combined candidate records the commit as an ancestor but explicitly excludes that unsafe production path. These observations are release blockers, not inferred workflow state.

## Executive reconciliation

The audit contains **81 detailed finding IDs**: 80 were open at the audit cutoff and `I18N-01` was already resolved. Its `H-01`–`H-17` register is a summary alias layer over those detailed findings, not 17 additional defects. The current implementation classification is:

| Status | Count | Meaning |
|---|---:|---|
| Remediated | 62 | 61 have a concrete remediation in this branch; `I18N-01` was already resolved in the audit baseline. None of the branch work is live until reviewed, merged, and deployed. |
| Mitigated / pending activation | 16 | The branch materially reduces the risk, but the original concern is only partly closed, depends on protected production activation, or still needs owner/external work. |
| Residual | 3 | The materially open findings are `OPS-08` (DNS/mail controls), `PRIV-03` (truthful legal/operator facts), and `CNT-04` (high-stakes editorial review). |
| **Total** | **81** | Complete one-to-one mapping of audit finding IDs. |

This is not a claim that the website is “100% fact-checked,” legally complete, deployed, or certified across browsers and assistive technology. The implementation establishes a detailed, testable remediation baseline and makes remaining work explicit. The source corpus contains **3,553 cards**: four carry legacy source-review metadata, while the stricter evidence gate finds **0 of 3,553 evidence-complete** because those four records lack the required claim-level evidence and approvals. The deployable public projection contains **3,275 cards in 51 categories** and quarantines all **278 high-priority safety-sensitive cards** across five categories. Quarantine is a runtime safety control, not editorial verification; the full corpus and its Git history remain accessible while the repository is public. The generated queue contains **382 review packets**, and **65 heuristic mutable-language candidates** await human disposition.

### High-severity register crosswalk

| Summary ID | Detailed finding(s) | Current status | Reconciliation |
|---|---|---|---|
| H-01 | OPS-01 | **Mitigated / pending activation** | Two-phase compatible release and exact Worker rollback reduce the failure window; D1 is still not transactionally atomic with deployment. |
| H-02 | OPS-02 | **Mitigated / pending activation** | Time Travel evidence and guarded drills exist; an encrypted off-account backup is still unproved. |
| H-03 | OPS-03, OPS-04 | **Mitigated / pending activation** | API-triggered monitoring and issue routing exist; full production synthetic/telemetry coverage and alert-channel ownership remain. |
| H-04 | API-01 | **Mitigated / pending activation** | Claims are now accurately limited to server checking, not human/proctored verification; public-answer automation remains possible. |
| H-05 | PRIV-02 | **Remediated in branch** | Audit actor deletion/export/anonymization conflict is addressed by migration and route changes. |
| H-06 | AUTH-01 | **Remediated in branch** | One-time recovery codes, reset, rotation, and session revocation are implemented. |
| H-07 | CNT-01 | **Mitigated / pending activation** | Per-card governance replaces unverifiable totals; the deployable projection is truthful, while 0/3,553 records are evidence-complete. |
| H-08 | SEO-01 | **Remediated in branch** | Crawlable pagination covers every card once per language. |
| H-09 | ENG-01 | **Remediated in branch** | Storage failure falls back to a non-persistent in-memory view without aborting initialization. |
| H-10 | UX-01 | **Remediated in branch** | Quick Fire is answer-hidden multiple choice with single-winner timing/race handling. |
| H-11 | UX-02 | **Remediated in branch** | Capability recovery and minimal session probing replace the one-shot global health bit. |
| H-12 | UX-03 | **Remediated in branch** | Unlock copy now distinguishes account access from correctness gates. |
| H-13 | A11Y-01, A11Y-02, A11Y-03 | **Remediated in branch** | Shared card/filter/search/modal focus and visible-focus contracts are implemented. |
| H-14 | A11Y-04 | **Remediated in branch** | Dynamic game controls/dialog focus and labels are implemented; physical assistive testing remains. |
| H-15 | RWD-01 | **Remediated in branch** | Fixed-layer sequencing and viewport reservation prevent the reproduced overlap. |
| H-16 | RWD-02 | **Remediated in branch** | Hanabi’s five stacks remain reachable in the narrow matrix. |
| H-17 | CNT-04 | **Residual** | All 278 high-stakes cards are excluded from the deployable site and API, but 0/278 have completed qualified review and the public repository/history still contains their source. |

## Verification record

The following evidence keys are used in the finding tables.

| Key | Repeatable evidence | Current result |
|---|---|---|
| V-REL | [`scripts/api-release-receipt.test.mjs`](../scripts/api-release-receipt.test.mjs), [`scripts/monitor-production.test.mjs`](../scripts/monitor-production.test.mjs), [`scripts/route-production-alert.test.mjs`](../scripts/route-production-alert.test.mjs), [`scripts/static-api-release-gate.test.mjs`](../scripts/static-api-release-gate.test.mjs), and [`scripts/release-manifest.test.mjs`](../scripts/release-manifest.test.mjs) | All release-control tests within V-CONTRACT pass, including 9/9 static/API gate cases and 21/21 monitor/incident-router cases. Production lanes share `jakh-production-release`, refuse stale/unprotected/unreviewed dispatches, bind exact live Worker/static identities, and reject any parallel SSH/EC2 or nondeterministic artifact path. API compatibility may run without D1 mutation; `migrate-final` is mechanically blocked until a same-commit compatibility receipt and tested encrypted off-account backup are enforced. Local results do not prove deployment. |
| V-STATIC | `node scripts/validate-static.mjs`; `node scripts/validate-bilingual.mjs`; both generators with `--check` | 427 HTML pages; 56 categories; 3,553 cards; 118 app pages; 112 localized topic pages; 16 physical shared Arabic routes; generated output current. |
| V-SEO | [`scripts/validate-seo.mjs`](../scripts/validate-seo.mjs), [`scripts/seo-pagination.test.mjs`](../scripts/seo-pagination.test.mjs), and [`scripts/seo-validation-contract.test.mjs`](../scripts/seo-validation-contract.test.mjs) | 427 HTML files, 424 canonical pages, 392 valid leaf quizzes, and 424 sitemap URLs; every card appears exactly once per language across crawlable topic pagination. |
| V-CONTRACT | Source/behavior suites under [`scripts/`](../scripts/) covering accessibility, Arabic routes, content review, frontend behavior, performance, provenance, publication quarantine, release binding, responsive layout, runtime monitor proof, search sharding, SEO, service-worker behavior, synthetic monitoring, static/API gating, and domain verification | 129/129 current source-contract tests pass, zero skipped/todo. The exact release artifact reproduced the browser and accessibility portions below. |
| V-CONTENT | [`scripts/content-review-report.mjs`](../scripts/content-review-report.mjs), [`scripts/content-review.test.mjs`](../scripts/content-review.test.mjs), [`scripts/generate-content-review-work-queue.mjs`](../scripts/generate-content-review-work-queue.mjs), [`scripts/publication-quarantine.test.mjs`](../scripts/publication-quarantine.test.mjs), strict question-bank and catalog checks | 3,553 structurally valid source cards; four legacy source-reviewed but **0 evidence-complete**; 3,549 source records pending; 278/278 high-stakes pending and 0/278 evidence-complete; 65 mutable-language candidates; 382 deterministic review packets. The deployable projection contains 3,275 cards and excludes all 278 high-stakes IDs, routes, datasets, search records, history UI records, and held-named assets. The intentional editorial `--complete` gate still fails until every record is genuinely complete. |
| V-IMG | [`scripts/validate-responsive-images.mjs`](../scripts/validate-responsive-images.mjs) | 17 JPEG source/thumbnail pairs and seven corrected WebP filenames validate; thumbnail bytes are 75% lower. |
| V-GAME | [`scripts/validate-game-focus.mjs`](../scripts/validate-game-focus.mjs), [`scripts/validate-game-responsive.mjs`](../scripts/validate-game-responsive.mjs), responsive and accessibility contracts | Focus contracts pass; the remediation browser run covered 24 game/viewport combinations. CI will rerun these checks from the release commit. |
| V-WORKER | `npm run test:coverage` and `npm run test:integration` in [`worker/`](../worker/) | 164/164 Worker tests pass; coverage is 85.56% lines, 72.47% branches, 91.13% functions against enforced 80/70/85 thresholds. Real Wrangler 4.116 local D1 migrations 0001–0008 and auth/recovery/privacy/scoring/Battle WebSocket integration pass, including exact Worker identity on HTTP/WebSocket paths, held-content API denial, a seeded retention backlog, named scheduled-job failure, backlog drain/recovery, scheduled maintenance, and deletion. |
| V-BROWSER | [`scripts/browser-matrix.mjs`](../scripts/browser-matrix.mjs) and [`scripts/browser-regression.mjs`](../scripts/browser-regression.mjs), wired into the release workflows | The exact built artifact `463ca419ecb12ccc90a931462635f03d689775ab421e0c026e6915579a677ead` passed 12/12: the same four regression scenarios in Chromium, Firefox, and WebKit, including its edge headers, fingerprint graph, lazy loading, mobile fixed UI, and cold-offline direct entry. This is automated engine coverage, not proof of physical Safari or mobile-device behavior; release-commit CI must reproduce it. |
| V-A11Y | [`scripts/accessibility-browser.mjs`](../scripts/accessibility-browser.mjs) and [`scripts/accessibility-contracts.test.mjs`](../scripts/accessibility-contracts.test.mjs) | The same exact artifact passed automated Chromium/Axe coverage over 20 standard routes, 20 forced-color routes, 20 200%-equivalent reflow checks, one completed-daily state, and ten authenticated OWNER-admin tab/language states, totaling 1,321 Axe rule passes. This does not constitute WCAG certification or a screen-reader/physical-device test. |
| V-PERF | [`scripts/validate-performance-budgets.mjs`](../scripts/validate-performance-budgets.mjs), [`scripts/performance-budget.test.mjs`](../scripts/performance-budget.test.mjs), and [`docs/PERFORMANCE-BUDGETS.md`](PERFORMANCE-BUDGETS.md) | All budgets pass. Initial `app.js` + `styles.css` is 369,511 raw / 88,548 gzip / 71,240 Brotli bytes; `app.js` alone is 261,355 / 67,727 / 53,987. Lazy Search/Leaderboard is 32,246 JS + 7,180 CSS raw bytes; lazy Battle is 27,547 JS + 9,942 CSS. English/Arabic search shards load only for active-language search. |
| V-SITE | [`scripts/build-static-site.mjs`](../scripts/build-static-site.mjs), [`site-worker/src/site-edge.js`](../site-worker/src/site-edge.js), [`scripts/site-release-receipt.mjs`](../scripts/site-release-receipt.mjs), and the [`site-worker/tests/`](../site-worker/tests/) suite | Repeated builds deterministically produced build `463ca419ecb12ccc90a931462635f03d689775ab421e0c026e6915579a677ead`, source graph `4b979444ad89823c33f8c92e58506dc4999ef0e3b6f7db5c252419c8fda63f19`, offline cache `sg-4b979444ad89823c33f8c92e58506dc4999ef0e3b6f7db5c252419c8fda63f19`, and exactly 596 files / 26,697,718 bytes. The projection has 397 HTML files, 394 sitemap URLs, 51 categories, and 3,275 cards; the manifest inventory matches disk 596/596 with zero extras or missing files. Both static workflows test the exact artifact before upload. All 21/21 site-worker/build/receipt tests and the Wrangler dry-run pass. Exact-version rollback, one-hop redirects, stable fallback for prior fingerprint URLs, headers on success/redirect/error/304 paths, normalization-safe held-route denial, and digest-valid immutable caching are implemented. No `jakh-site` production baseline, DNS cutover, header activation, or rollback exercise is evidenced. |
| V-DOMAIN | [`scripts/verify-domain-controls.mjs`](../scripts/verify-domain-controls.mjs), [`scripts/verify-domain-controls.test.mjs`](../scripts/verify-domain-controls.test.mjs), and [`docs/DOMAIN-CONTROLS.md`](DOMAIN-CONTROLS.md) | 9/9 deterministic verifier tests pass, including strict RSA/Ed25519 DKIM key validation. The live strict gate fails: no DNSSEC/DS or CAA; SPF ends `~all`; no operator-supplied active DKIM selector and a misplaced apex `v=DKIM1` record; DMARC is `quarantine` with relaxed alignment; MTA-STS and TLS-RPT are absent. Apex/`www`/API routing, HTTPS, and published IPv6 checks pass. |

## Finding-by-finding ledger

### Operations, deployment, and infrastructure

| ID | Status | Implementation evidence | Verification evidence | Remaining owner or external action |
|---|---|---|---|---|
| OPS-01 | **Mitigated / pending activation** | [`api-deploy.yml`](../.github/workflows/api-deploy.yml) separates a schema-tolerant compatibility Worker deployment from a later `migrate-final` run, proves the exact active commit before D1 mutation, records pre/post state, and has exact Worker rollback when a trustworthy predecessor identity exists. [`api-release-receipt.mjs`](../scripts/api-release-receipt.mjs) rejects split traffic, unsupported schema, ambiguous D1 state, or an unproven rollback target. Worker feature gates support schemas 6–8. `migrate-final` currently stops before D1 mutation until the compatibility/backup prerequisites are mechanically enforced. | V-REL; V-WORKER. | Protect the `production` environment and deploy compatibility only. Establish the tested encrypted off-account backup and same-commit receipt before enabling `migrate-final`. D1 migration is still not transactionally atomic with Worker deployment and destructive data rollback is intentionally not automatic. |
| OPS-02 | **Mitigated / pending activation** | [`recovery-verification.yml`](../.github/workflows/recovery-verification.yml), [`worker/scripts/recovery-evidence.mjs`](../worker/scripts/recovery-evidence.mjs), its tests, and [`worker/RECOVERY.md`](../worker/RECOVERY.md) add weekly read-only Time Travel evidence and a guarded quarterly restore/undo drill. The non-production D1 database `jakh-recovery-drill` exists as ID `704609ac-4adc-411b-8a9c-894da397ee43` in EEUR; the deploy receipt can capture a pre-migration bookmark. | Recovery safeguard tests are part of V-WORKER; workflow guards require protected `main`, the exact drill name/ID, and explicit confirmation. Database creation is resource setup only: no completed export, import, restore, undo, checksum, RPO, or RTO receipt exists. | **Create and prove a current encrypted off-account export/backup.** Name its destination, retention, RPO/RTO, and key custodian; restore it into the isolated drill database; verify checksums and application behavior; retain the signed result. Never bind the drill database to the production Worker. |
| OPS-03 | **Mitigated / pending activation** | [`production-monitor.yml`](../.github/workflows/production-monitor.yml) now runs every 30 minutes and after both Pages and API workflows. [`route-production-alert.mjs`](../scripts/route-production-alert.mjs) opens/deduplicates a repository issue, records repeat failures and recovery, then closes the incident. | V-REL. | Merge and exercise the issue route with repository permissions. Decide whether a GitHub issue is sufficient for the response objective; configure an independently monitored pager/email/webhook if required. Private Cloudflare alerts remain unverified. |
| OPS-04 | **Mitigated / pending activation** | [`monitor-production.mjs`](../scripts/monitor-production.mjs) validates the health/schema/features contract, public server-checked leaderboard truthfulness, CORS, pages/assets, and structured results. [`synthetic-account-monitor.mjs`](../scripts/synthetic-account-monitor.mjs) defines a bounded disposable-account path. [`worker/scripts/local-integration.mjs`](../worker/scripts/local-integration.mjs) exercises migrated D1 auth, recovery, consent, privacy requests, scoring cancellation, a real Battle WebSocket, seeded cleanup backlog, named scheduled failure, recovery after draining the backlog, logout, and deletion. | V-REL; V-WORKER. | Local integration proves the paths, not production monitoring. Configure synthetic credentials/ownership and exercise the production runner without a real user account; add external DNS/TLS expiry, capacity, saturation, error-rate, and latency telemetry under an approved retention and alert policy. |
| OPS-05 | **Mitigated / pending activation** | API receipts bind source commit, Worker deployment/version, observed schema, migration list, health contract, and rollback target. [`release-manifest.mjs`](../scripts/release-manifest.mjs) binds the full source commit to exact static/API artifact inventories and migrations. [`static-site.yml`](../.github/workflows/static-site.yml) verifies exactly one 100%-traffic live API deployment from the same commit on healthy schema 8 immediately before and after the static deploy; a failed post-deploy proof invokes exact static rollback. | V-REL; V-STATIC; V-SEO; V-CONTRACT; V-WORKER; V-SITE. This is fail-closed workflow proof, not a live deployment receipt. | Protect the release environments and provision a least-privilege `CLOUDFLARE_API_RELEASE_READ_TOKEN`. The static and API Workers remain separately deployed systems, and the API workflow does not deploy from the static workflow's retained API dry-run inventory. Retain the 3/3-verified cross-artifact manifest with both deployment receipts and prove both live versions match it. |
| OPS-06 | **Mitigated / pending activation** | [`static-site.yml`](../.github/workflows/static-site.yml), [`build-static-site.mjs`](../scripts/build-static-site.mjs), [`site-release-receipt.mjs`](../scripts/site-release-receipt.mjs), and [`site-worker/README.md`](../site-worker/README.md) implement a deterministic static artifact, exact prior Worker-version capture, post-deploy smoke proof, automatic exact-version rollback for normal releases, and a fail-closed one-time baseline cutover/legacy-DNS rollback runbook. | V-SITE. The repository proves the rollback mechanism and artifact boundary locally; it does not contain a successful production rollback receipt. | Approve and execute the one-time `jakh-site` baseline cutover with pre-change DNS/HTTP snapshots, a named rollback operator, retained legacy Pages state, least-privilege credentials, and an approved maintenance window. Exercise and retain an exact-version rollback before treating this as operationally closed. |
| OPS-07 | **Mitigated / pending activation** | [`site-worker/src/site-edge.js`](../site-worker/src/site-edge.js) applies HSTS, CSP with `frame-ancestors`, `nosniff`, Referrer-Policy, Permissions-Policy, and explicit framing protection to successful assets, redirects, `404`, conditional `304`, and Worker-error responses. | V-SITE validates the response classes locally; current public GitHub Pages responses do not demonstrate these controls. | Activate the reviewed static Worker through the guarded cutover, then verify the apex, `www`, admin/privacy pages, `404`, `304`, and error paths from outside Cloudflare. Decide HSTS preload separately only after every subdomain is ready. |
| OPS-08 | **Residual** | [`verify-domain-controls.mjs`](../scripts/verify-domain-controls.mjs) supplies a repeatable read-only DNS/mail gate, and the static Worker contains an intentionally disabled MTA-STS capability without invented MX or report recipients. No DNS or mail control was changed. | V-DOMAIN: routing, HTTPS, and IPv6 pass; the live strict security/mail gate fails on DNSSEC/DS, CAA, SPF, DKIM evidence/placement, DMARC enforcement/alignment, MTA-STS, and TLS-RPT. | Domain/mail owners must provide the complete sender inventory, actual DKIM selector(s), monitored aggregate/report recipients, and rollout/rollback authority. Then stage DS/DNSSEC, CAA, safe SPF `-all`, valid selector records, DMARC `reject` with strict alignment, MTA-STS, and TLS-RPT while monitoring delivery. Do not guess records or recipients. |
| OPS-09 | **Remediated in branch** | [`.well-known/security.txt`](../.well-known/security.txt) is present at the exact deployable path and included in the deterministic static manifest; the guarded legacy Pages builder uses the same artifact boundary. | V-STATIC; V-SITE; the production monitor includes the standard discovery path. | Deploy the approved static host, then confirm `https://jakh.net/.well-known/security.txt` returns `200`, the correct media type/body, and a valid canonical/expiry. |

### Security and backend integrity

| ID | Status | Implementation evidence | Verification evidence | Remaining owner or external action |
|---|---|---|---|---|
| SEC-01 | **Remediated in branch** | [`worker/src/security.ts`](../worker/src/security.ts) and [`worker/src/password-hasher.ts`](../worker/src/password-hasher.ts) create PBKDF2-HMAC-SHA256 hashes at 600,000 iterations while accepting the legacy 100,000 format for padded verification/upgrade compatibility. | [`worker/tests/password-hasher.test.mjs`](../worker/tests/password-hasher.test.mjs), [`worker/tests/account-security.test.mjs`](../worker/tests/account-security.test.mjs); V-WORKER. | Deploy the Worker and monitor CPU/latency. Existing hashes upgrade through authenticated use; set a policy for dormant legacy hashes. |
| API-01 | **Mitigated / pending activation** | [`worker/src/verified-scoring.ts`](../worker/src/verified-scoring.ts), [`app.js`](../app.js), and [`privacy.html`](../privacy.html) consistently use **server-checked**, declare `proctored: false`, include an automation disclaimer, remove speed tiebreaks, and separate server scoring from editorial fact review. Canonical `/server-checked/` routes retain legacy aliases. | [`worker/tests/verified-scoring.test.mjs`](../worker/tests/verified-scoring.test.mjs), [`scripts/frontend-product-contract.test.mjs`](../scripts/frontend-product-contract.test.mjs), production monitor; V-WORKER and V-CONTRACT. | Public answers still make lookup/automation possible. Do not market the score as proof of a human, proctoring, anti-cheat, or factual verification. Stronger assurance would require a different product/threat model. |
| API-02 | **Remediated in branch** | Password change now atomically deletes all sessions and inserts a fresh current-session token; recovery reset also revokes prior sessions. | [`worker/tests/account-security.test.mjs`](../worker/tests/account-security.test.mjs); V-WORKER and real integration recovery flow. | Deploy and verify cookie rotation with a non-production account; revoke old sessions if deployment detects an anomaly. |
| API-03 | **Remediated in branch** | Challenge creation uses an atomic conditional insert with one pending challenge per user/category. Exact cancellation validates user, category, challenge ID, token hash, and pending state; submit/cancel have a single winner. | [`worker/tests/verified-scoring.test.mjs`](../worker/tests/verified-scoring.test.mjs), [`worker/tests/index.test.mjs`](../worker/tests/index.test.mjs), frontend cancellation contracts; V-WORKER. | Deploy and monitor `409 ACTIVE_CHALLENGE` and cancellation rates. Keep category-only discard limited to the explicit reload-recovery warning path. |
| API-04 | **Remediated in branch** | Registration batches the user, one-way recovery-code digest, and session insert so the response cannot claim failure after creating only the account. Plaintext recovery codes are returned once and never stored. | [`worker/tests/account-security.test.mjs`](../worker/tests/account-security.test.mjs), migration tests, and V-WORKER real D1 registration. | Deploy migrations 0007–0008 through the gated release and verify one controlled registration before broad activation. |
| API-05 | **Remediated in branch** | Stateful streak calculation is exposed only as `POST /api/user/streak`; GET cannot dispatch it. [`app.js`](../app.js) calls POST. | [`worker/tests/index.test.mjs`](../worker/tests/index.test.mjs); V-WORKER. | Deploy and watch for obsolete clients calling GET; no data migration is required. |

### Privacy and authentication

| ID | Status | Implementation evidence | Verification evidence | Remaining owner or external action |
|---|---|---|---|---|
| PRIV-01 | **Remediated in branch** | [`privacy-consent.js`](../privacy-consent.js), [`privacy-page.js`](../privacy-page.js), [`privacy.html`](../privacy.html), and consent version 2 make analytics denied by default, distinguish device from account analytics, disable future GA after revocation, remove the injected loader/known state where possible, and explicitly explain that already-executed code cannot be undone without reload. Old notice consent no longer authorizes account analytics. | [`worker/tests/privacy.test.mjs`](../worker/tests/privacy.test.mjs), privacy/frontend contracts; V-WORKER and V-CONTRACT. | Deploy, then manually verify allow → deny → reload in a fresh browser and confirm network/cookie behavior against the live GA property. |
| PRIV-02 | **Remediated in branch** | [`0008_audit_log_retention.sql`](../worker/migrations/0008_audit_log_retention.sql) makes audit actors nullable with `ON DELETE SET NULL`; deletion anonymizes target references. [`worker/src/privacy.ts`](../worker/src/privacy.ts) exports redacted audit activity and discloses audit/recovery data boundaries. | Migration and privacy tests; V-WORKER real account deletion. | Deploy migration 0008 only after the compatibility phase. Verify existing audit rows migrate as intended with a protected database snapshot. |
| PRIV-03 | **Residual** | The privacy notice is more complete operationally, but the branch cannot invent the legal operator/controller, postal/privacy contact, governing law, complaint route, international-transfer basis, minimum age/parental-consent rule, or Terms acceptance. | Public-document review only; this requires owner facts and legal review. | Owner/legal counsel must supply the actual legal entity and jurisdictional policy, decide the minors/age model and Terms, approve translations, and define registration acknowledgement. Do not deploy fabricated placeholders. |
| PRIV-04 | **Remediated in branch** | A dedicated, rate-limited `POST /api/privacy/requests` route and bilingual form replace the suggestion-box instruction. Account linkage occurs only after explicit opt-in; anonymous requests never inspect a session. No unsupported response-time promise is made. | [`worker/tests/routes.test.mjs`](../worker/tests/routes.test.mjs), [`privacy-page.js`](../privacy-page.js); V-WORKER real anonymous privacy request. | Deploy and assign a real owner/inbox/process for reviewing requests. Add a public contact only after the owner confirms it is monitored. |
| PRIV-05 | **Remediated in branch** | [`privacy.html`](../privacy.html) now discloses language, consent, progress/favorites, trial/daily state, install dismissal, app version, cache/offline data, and the scoped mutation queue; clear-device behavior is distinguished from account deletion. | Privacy/frontend contracts; V-CONTRACT. | Re-audit disclosure whenever a new local/session/cache key or offline payload is introduced. |
| PRIV-06 | **Remediated in branch** | Retention wording now describes eligibility targets, retries, and possible backlog delay rather than absolute deletion guarantees. [`worker/src/db.ts`](../worker/src/db.ts) drains up to ten 500-row batches and uses a final read-only `LIMIT 1` probe so exactly 5,000 deletions are not falsely reported as backlog. | [`worker/tests/db.test.mjs`](../worker/tests/db.test.mjs), [`worker/tests/privacy.test.mjs`](../worker/tests/privacy.test.mjs); V-WORKER. | Deploy and add operational backlog/age metrics if absolute service objectives are later promised. |
| AUTH-01 | **Remediated in branch** | Migration 0007 adds recovery-code digests. Registration shows a one-time code; reset consumes it, revokes sessions, signs the user in, and returns a replacement; signed-in users can rotate it with current-password confirmation. UI is bilingual and blocks accidental dismissal before save acknowledgement. | [`worker/tests/account-security.test.mjs`](../worker/tests/account-security.test.mjs), migration tests, V-WORKER real one-time recovery/rotation. | Deploy and update support guidance. Recovery remains code-based, not email-based; users must securely retain the code. |
| AUTH-02 | **Remediated in branch** | [`app.js`](../app.js) adds password confirmation, `autocomplete="email"`, recovery-loss explanation, one-time recovery receipt, and a direct privacy/account-data link in registration. | Accessibility and frontend contracts; V-CONTRACT. | Terms/age acknowledgement remains blocked on the real owner/legal decisions in PRIV-03. |
| AUTH-03 | **Remediated in branch** | Main and admin clients retain the visible signed-in state and show an explicit failure if server logout fails; success is announced only after the API confirms logout. | [`app.js`](../app.js), [`admin.js`](../admin.js), frontend contracts; V-CONTRACT. | Deploy and test an intentional offline/logout failure with a non-production session. |

### SEO, content, and editorial trust

| ID | Status | Implementation evidence | Verification evidence | Remaining owner or external action |
|---|---|---|---|---|
| SEO-01 | **Remediated in branch** | [`generate-seo-pages.mjs`](../scripts/generate-seo-pages.mjs) emits crawlable English `/{slug}/page/{n}/` and Arabic `/ar/topics/{slug}/page/{n}/` slices beyond page 1, with self-canonical metadata, unique descriptions, prev/next navigation, reciprocal hreflang, accessible static Q&A/provenance, and links back to the interactive page. [`sitemap.xml`](../sitemap.xml) includes every canonical slice. | V-SEO proves every one of the 3,553 IDs occurs exactly once per language across the indexable topic pagination set. | Deploy Pages, submit/observe the 424-URL sitemap, and monitor crawl/index coverage. Do not add `/page/1/` aliases. |
| SEO-02 | **Remediated in branch** | [`generate-arabic-routes.mjs`](../scripts/generate-arabic-routes.mjs) creates physical Arabic equivalents for Home, Mind Lab, Collections, Game Hub, About, Privacy, and all ten games. English/Arabic pages use self canonicals and reciprocal `en`, `ar`, and `x-default` alternates. Runtime language selectors navigate clean physical routes. | [`scripts/arabic-routes.test.mjs`](../scripts/arabic-routes.test.mjs), V-STATIC, V-SEO, and V-CONTRACT. | Deploy and verify all 16 pairs live, including storage-blocked route selection and Arabic internal navigation. |
| SEO-03 | **Mitigated / pending activation** | Canonicals and internal links point to clean URLs. [`site-worker/src/site-edge.js`](../site-worker/src/site-edge.js) implements direct one-hop `301` normalization for physical `.html` and redundant `/index.html` aliases, including simultaneous HTTP/`www` normalization, query-string retention, and loop avoidance. | V-SEO; V-SITE. The edge tests prove rule behavior, but the current GitHub Pages origin still serves duplicate `200` aliases. | Activate the reviewed static Worker, then crawl-test every alias and redirect combination from production. Keep canonicals as a secondary signal; do not call this closed from source tests alone. |
| SEO-04 | **Remediated in branch** | Generated/static navigation and runtime language controls use physical routes and remove only the retired `lang` parameter while preserving unrelated query state. | Arabic/SEO contracts reject internal `?lang=` links; V-SEO and V-CONTRACT. | Deploy, then crawl for query-language variants and watch Search Console canonical selection. |
| SEO-05 | **Remediated in branch** | Topic and collection pages now use real category/collection raster artwork for `og:image`/Twitter metadata with byte-true MIME and dimensions; a generic fallback is retained only where no differentiated artwork exists. | SEO quality contracts sniff the image bytes and reject false MIME/dimensions; V-SEO. | Deploy and sample cards through social debuggers. Create additional art only if shared-page differentiation is a product requirement. |
| CNT-01 | **Mitigated / pending activation** | Every source card has deterministic per-card `review` metadata. The UI and catalog distinguish **server-scorable** from **source-reviewed**; misleading category-level “verified” totals were removed. [`content-review-lib.mjs`](../scripts/content-review-lib.mjs), the evidence schema/report, CI, visible provenance, and a deterministic **382-packet** work queue establish an auditable workflow without auto-promoting a card. Accepted proof must resolve to a real regular file beneath the declared proof root and match its recorded SHA-256; traversal, symlinks, missing/non-files, and mismatches fail closed. | V-CONTENT: 3,553 valid source records, four legacy source-reviewed, but **0/3,553 evidence-complete**; the deployable projection contains 3,275 cards. | This fixes the unverifiable counting model, not the corpus backlog. Roster qualified reviewers; add reciprocal claim/evidence mappings, exact locators, dates, mutable assessments, bilingual and final approvals; then intentionally update public metadata. Do not claim complete factual verification until the strict gate passes. |
| CNT-02 | **Remediated in branch** | `football-138` now names Harry Kane; `currencies-26` now asks/answers the Kuwaiti dinar subdivision correctly in both languages. Both carry dated authoritative sources. | V-CONTENT reviewed-card checks and strict data validation. | Editorial owner should periodically revalidate mutable sports records and keep source URLs current. |
| CNT-03 | **Remediated in branch** | `cin-37` now correctly identifies Roger Deakins without the self-contradictory wording; `history-038` uses historically responsible Genoese/Spanish-sponsored Caribbean/Taíno framing with sources. | V-CONTENT and bilingual/static validation. | Retain editorial review for any future wording change. |
| CNT-04 | **Residual** | [`production-quarantine.json`](content-review/production-quarantine.json) deterministically holds the complete law, medical, pharmacy, economics/finance, and survival categories. Static build, client, service worker, edge, API catalog/scoring/Battle, monitoring, history projection, and receipts all fail closed for those 278 IDs and five category routes, including encoded and normalization variants. | V-CONTENT and V-SITE: **0 of 278 high-stakes cards are reviewed; all 278 are absent from the 3,275-card deployable projection.** Red-team probes covered 105 hostile route variants and 15 held-asset variants with zero disclosure. | Qualified domain reviewers must verify each claim against current authoritative guidance before re-entry. Runtime quarantine does not remove the source from this public repository or history; the owner must choose private-source/history handling or explicitly accept that exposure. |
| CNT-05 | **Mitigated / pending activation** | Review tooling flags mutable/superlative/current-state language, records match reasons without treating the heuristic as a truth verdict, and supports `validAsOf`, `reviewDueAt`, and overdue reopening. | V-CONTENT identifies 65 candidates and zero stale or overdue reviewed cards. | Human reviewers must resolve all 65 candidates, set suitable recheck intervals, and schedule recurring revalidation. The heuristic includes false positives and must not auto-edit facts. |
| CNT-06 | **Remediated in branch** | The misspelled `subscategory` field and confirmed trailing-space defect were removed. The three identified overlong prompts were shortened bilingually without dropping their tested answer meaning, and generated search/SEO output was refreshed. | V-STATIC; V-SEO; V-CONTENT and bilingual/content contracts prevent the known defects from recurring. | Normal editorial review still applies to future copy changes; no known item from this finding remains open. |

### Accessibility and localization

| ID | Status | Implementation evidence | Verification evidence | Remaining owner or external action |
|---|---|---|---|---|
| A11Y-01 | **Remediated in branch** | Card flips, subcategory changes, and filter rerenders restore a deterministic equivalent control instead of leaving focus on `BODY`. Selected filters expose programmatic state. | Accessibility contracts and V-BROWSER focus scenario; V-CONTRACT. | Confirm with keyboard-only users in supported browsers after deployment. |
| A11Y-02 | **Remediated in branch** | Shared auth/profile/leaderboard/search/paywall/completion dialogs use centralized open/close focus management, Escape, trapping, and opener restoration. Catan trade flow follows the same lifecycle. | Accessibility/frontend contracts and V-BROWSER modal scenario. | Verify authenticated/profile/admin variants and screen-reader announcements on production. |
| A11Y-03 | **Remediated in branch** | Search-result, privacy, and shared-control focus styles retain visible contrast-safe indicators, including a privacy focus ring with at least 3:1 contrast against adjacent colors. | [`accessibility-contracts.test.mjs`](../scripts/accessibility-contracts.test.mjs); V-CONTRACT; V-A11Y includes 20 automated forced-color routes. | Validate the focus treatment with supported physical browsers and screen-reader/keyboard users. |
| A11Y-04 | **Remediated in branch** | SET, Mastermind, Codenames, and Catan preserve or deliberately move focus after dynamic replacement; Catan selects are labelled and its dialog is managed. | V-GAME and accessibility contracts. | Complete physical keyboard/screen-reader playthroughs for all ten games. |
| A11Y-05 | **Remediated in branch** | Subcategory/filter controls expose pressed/selected state in markup and preserve it through rerenders. | Accessibility contracts; V-CONTRACT. | Check announcement wording with NVDA, JAWS, VoiceOver, and TalkBack. |
| A11Y-06 | **Remediated in branch** | Undefined public color tokens were replaced with defined contrast-safe tokens; low-opacity completed-daily styling was removed; the completed badge and privacy focus treatment use contrast-safe states. | Accessibility contracts reject the prior token/opacity patterns; V-A11Y includes the completed-daily state, 20 standard routes, 20 forced-color routes, and authenticated OWNER-admin states. | Automated Axe coverage is not a human visual or assistive-technology certification. Verify platform-specific hover, disabled, error, high-contrast, and screen-reader states after deployment. |
| A11Y-07 | **Remediated in branch** | Registration email has correct autocomplete; permanent-deletion username/password fields have explicit labels, accessible names, and autocomplete semantics. | Accessibility contracts; V-CONTRACT. | Verify the destructive flow with assistive technology after a non-production deployment. |
| I18N-01 | **Remediated before this branch** | The audit baseline had already replaced English-href CSS artwork matching with actual localized image elements. Current generators preserve Arabic topic artwork and physical Arabic route navigation. | V-STATIC, V-SEO, and Arabic contracts. | Regression monitoring only. This row is included for complete audit-ID accounting. |

**Cross-cutting accessibility limitation:** source tests, the Chromium/Axe audit, and the Chromium/Firefox/WebKit regression matrix do not constitute WCAG certification. The automated run covers forced colors and a 200%-equivalent reflow viewport, but physical Safari/iOS/Android, OS-level high-contrast behavior, actual 200–400% browser zoom, VoiceOver, TalkBack, NVDA, JAWS, touch accuracy, and human task completion remain formal acceptance work.

### UX, responsive behavior, and visual consistency

| ID | Status | Implementation evidence | Verification evidence | Remaining owner or external action |
|---|---|---|---|---|
| UX-01 | **Remediated in branch** | Quick Fire is now a four-option multiple-choice quiz; the answer stays hidden until one response, timeout cannot overwrite a completed answer, repeated clicks are guarded, and explicit review follows. | Frontend contracts and V-BROWSER race scenario; V-CONTRACT. | Deploy and validate timing/announcements with touch and keyboard users. |
| UX-02 | **Remediated in branch** | Health probing has bounded retry plus focus/online rechecks. A minimal `GET /api/auth/session` probe returns 200 with authenticated true/false and does not touch D1 for missing/malformed tokens. Transient failures preserve prior identity instead of hiding all cloud UI for the session. | Frontend contracts, Worker session tests, V-WORKER. | Deploy and observe slow/partial API behavior; tune budgets from real latency rather than hiding features globally. |
| UX-03 | **Remediated in branch** | Account-gating copy now distinguishes account access from correctness-based Hard/Very Advanced progression. | Truthfulness contract in [`frontend-product-contract.test.mjs`](../scripts/frontend-product-contract.test.mjs). | Product owner must preserve the distinction when changing unlock rules. |
| UX-04 | **Remediated in branch** | Category status copy separately reports rendered, matched, total, and remaining cards. | Frontend contract and browser filter/reset scenario. | Production visual check on small and large categories. |
| UX-05 | **Remediated in branch** | Reset removes only filter/share parameters and stale card state while preserving unrelated parameters such as campaign tags. | Frontend contract and V-BROWSER URL scenario. | Deploy and sample language switching/reload with mixed query parameters. |
| UX-06 | **Remediated in branch** | Search evaluates all index hits, ranks deterministically, reports the true total, and labels the displayed top 30. | Frontend contract and V-BROWSER search scenario. | The index is still large; address its transfer/architecture under PERF-01/PERF-02 rather than silently truncating again. |
| UX-07 | **Remediated in branch** | Revealing the daily answer no longer marks completion; explicit “knew it/review” outcomes persist state. | Frontend contract; V-CONTRACT. | Confirm analytics/product metrics use the new meaning after deployment. |
| UX-08 | **Remediated in branch** | Arabic auth/recovery UI preserves API validation/network distinctions with localized failure messages rather than mapping all failures to bad credentials. | Frontend/account contracts; V-CONTRACT. | Live-test representative 4xx, 429, 5xx, and offline paths. |
| UX-09 | **Remediated in branch** | The mobile bar has a real Games destination and marks Game Hub active without mislabelling Mind Lab/Explore. | Responsive layout contract; V-CONTRACT. | Verify both language routes on physical narrow devices. |
| UX-10 | **Remediated in branch** | Sharing is centralized through Web Share → clipboard → selectable manual fallback; cancellation and clipboard failure are handled. Arabic links use the correct physical route. | Frontend contract; V-CONTRACT. | Test Safari/iOS share sheets and permission-denied clipboard behavior. |
| UX-11 | **Remediated in branch** | Progress updates precede achievement evaluation so threshold achievements unlock on the triggering action. | Frontend product contract. | Monitor migrated/local progress edge cases. |
| UX-12 | **Remediated in branch** | User-facing copy describes Battle as an individual free-for-all room rather than a team mode. | Truthfulness contract. | If actual teams are later implemented, add team data/rules/tests before restoring the “team” claim. |
| UX-13 | **Remediated in branch** | Game marketing qualifies browser adaptations and Lite variants instead of claiming exhaustive canonical-rule implementations. | Truthfulness contract and generated/static copy validation. | Canonical rule completeness remains product work, especially Go ko/dead-stone adjudication and shallow AI. Do not restore stronger claims without rule suites. |
| UX-14 | **Remediated in branch** | Chess/Reversi copy distinguishes browser play, local friend turns, and local AI rather than implying remote multiplayer. | Truthfulness contract. | Add remote-online wording only with an implemented network path. |
| RWD-01 | **Remediated in branch** | Shared fixed-UI coordination sequences consent/install surfaces and reserves bottom-navigation/header space; short landscape removes sticky-header competition. | Responsive contracts, V-BROWSER 320×568 geometry, and prior 568×320 validation. | Recheck real safe areas, dynamic browser chrome, keyboard, and PWA-install behavior on physical iOS/Android. |
| RWD-02 | **Remediated in branch** | Hanabi stack layout adapts within 320 px so all five stacks remain reachable without global clipping. | V-GAME across the remediation viewport matrix. | Physical touch/zoom verification remains. |
| RWD-03 | **Remediated in branch** | Backgammon uses a discoverable responsive board/control treatment with 44 px precise alternatives instead of relying on an unannounced clipped board. | V-GAME. | Verify touch accuracy and scroll affordance on narrow physical devices. |
| RWD-04 | **Remediated in branch** | Brand and shared actions retain 44 px targets; Chess/Reversi expose 44 px precise controls when board cells cannot remain 44 px at the narrowest viewport. | Responsive contracts and V-GAME. | Physical touch testing remains required; board geometry alone does not establish motor accessibility. |
| VIS-01 | **Remediated in branch** | Consent and shared language components render Arabic copy/RTL state on the Arabic admin experience. | Arabic/accessibility contracts; V-CONTRACT. | Capture and review the live signed-out and signed-in admin states. |
| VIS-02 | **Remediated in branch** | Shared social controls retain an accessible visible/icon treatment at mobile widths rather than hiding their only text into blank circles. | Responsive layout contract. | Visual check on Collections in both languages. |
| VIS-03 | **Mitigated / pending activation** | Shared fixed-layer, control, focus, responsive-image, and loading behavior is more consistent; public undefined tokens and unsafe completed-state opacity were removed; privacy styles are extracted to a reusable sheet; Arabic admin typography/tracking and no-JavaScript Arabic accessible names are normalized. | V-CONTRACT; V-IMG; V-A11Y covers public, privacy, daily-complete, and authenticated OWNER-admin states in both languages. | Privacy/Admin retain deliberately distinct local systems, and density, skeleton, hierarchy, brand, and aesthetic judgments require owner/user visual review. Automated checks cannot approve subjective design consistency. |

### Performance, PWA/offline, engineering, and tests

| ID | Status | Implementation evidence | Verification evidence | Remaining owner or external action |
|---|---|---|---|---|
| PERF-01 | **Mitigated / pending activation** | Two invocation-only feature areas and their CSS were split from the initial route: Live Battle uses [`battle-mode.js`](../battle-mode.js)/[`battle-mode.css`](../battle-mode.css), while global Search/Leaderboard/server-checked challenge uses [`search-leaderboard.js`](../search-leaderboard.js)/[`search-leaderboard.css`](../search-leaderboard.css). The initial pair is 367,674 raw / 87,955 gzip / 70,810 Brotli bytes; the lazy Search pair is 32,246 + 7,180 raw bytes and Battle is 27,547 + 9,942. | V-PERF; V-CONTRACT; the browser matrix covers lazy search behavior. | The split is implemented and budgeted, but `app.js`/shared CSS remain substantial and no production field or repeatable network/CPU trace proves user impact. Deploy the exact graph, collect cold/warm field or lab evidence, and continue route ownership only where measurements justify it. |
| PERF-02 | **Remediated in branch** | `loadCatalog()` memoizes the in-flight promise. Search ranks all results truthfully and requests only [`search-index.en.json`](../data/search-index.en.json) or [`search-index.ar.json`](../data/search-index.ar.json) when the search feature opens; language switching refreshes the active shard without downloading both. | V-PERF budgets the 572,007-byte English and 791,598-byte Arabic raw shards; search-index contracts and V-BROWSER verify selection and behavior. | Deploy and measure parsing/main-thread cost. Keep the legacy compatibility index out of the new search path and preserve deterministic shard generation. |
| PERF-03 | **Remediated in branch** | Seven WebP-byte files were renamed from `.png` to `.webp`; references and social MIME metadata match. Seventeen large JPEG covers have 320w derivatives, valid `srcset`/`sizes`, and intrinsic dimensions. | V-IMG: 75% lower thumbnail bytes; V-SEO image sniffing. | Deploy and rerun cold/full-scroll image transfer measurements; add more sizes if real viewport/RUM data justifies them. |
| PERF-04 | **Mitigated / pending activation** | The static build retains stable compatibility URLs and emits SHA-256 fingerprinted app/CSS, privacy, lazy-feature, and language-shard assets; it rewrites the built dependency graph and derives the service-worker cache identity from the complete deployable source graph. The edge grants one-year `immutable` only when the embedded digest matches the served bytes; HTML, `sw.js`, metadata, and stable URLs revalidate. For nine known fingerprint families, stale hash-shaped URLs from already-open tabs fall back to the current stable asset with `no-store` and `X-JAKH-Compatibility-Fallback`; current fingerprints stay immutable and unknown families return `404`. | V-SITE validates fingerprint identity, transformed references, cache policy, deterministic manifest inventory, open-tab compatibility, and exact-version artifact rollback. V-PERF verifies source budgets. | None of this cache/compression policy is live on the current Pages origin. Activate the exact static Worker artifact, verify Brotli/ETag/cache headers and content types externally, measure transfers, and retain the manifest/rollback receipt. |
| PERF-05 | **Remediated in branch** | Anonymous startup uses the minimal `GET /api/auth/session` contract, which returns 200 `{authenticated:false}` for missing/malformed tokens without D1 work; profile loads only after authenticated state. | Worker session tests and V-WORKER integration. | Deploy and confirm anonymous pages no longer emit expected 401 console noise. |
| PWA-01 | **Remediated in branch** | [`game-i18n.js`](../game-i18n.js) registers `/sw.js` for all ten direct game entries through one shared path. | Service-worker contract checks all direct-game documents; V-CONTRACT and V-BROWSER offline/direct-game scenario. | Deploy and test a fresh profile entering each game directly. |
| PWA-02 | **Remediated in branch** | [`sw.js`](../sw.js) fails required installation closed, deletes a partial core cache, and guarantees the shared shell, six core English pages, all 16 physical Arabic shared-route equivalents, Science data, catalog/scripts, all ten game documents, dedicated canonical `/offline`, and install-day daily data. Arabic required routes use their trailing-slash canonicals so the exact edge artifact installs without redirect/404 ambiguity. Future daily data is optional warming only. | Seven service-worker contract scenarios within V-CONTRACT; V-BROWSER cold-offline English and Arabic scenarios against the exact artifact. | Confirm install size and quota behavior across supported browsers. This is a bounded cold-offline core, not a promise that all 56 datasets or cloud features work offline. |
| PWA-03 | **Remediated in branch** | Unmatched failed navigation returns bilingual [`offline.html`](../offline.html), never Home under an incompatible path. Compatible cached documents are used only for their normalized route. | Service-worker contracts and V-BROWSER. | Deploy and test uncached topic/game/admin URLs offline. |
| PWA-04 | **Mitigated / pending activation** | Navigation/assets/data caches are capped at 64/96/64; same-origin version queries normalize; mutation queues are capped at 100, expire after seven days, retry only transient classes, serialize by user/key, and clear on logout; senderless `CLEAR_CACHE`/`SKIP_WAITING` paths and app-version cache erasure were removed. Install dismissal now expires deterministically after **30 days**, allowing the prompt to become eligible again. | Service-worker and frontend product contracts; V-CONTRACT; V-BROWSER covers all three automated engines. | Browser-specific install affordance remains platform-dependent. Validate storage pressure, multi-account separation, real Safari/iOS/Android install behavior, dismissal resurfacing, and quota eviction on physical devices. |
| ENG-01 | **Remediated in branch** | Safe storage wrappers fall back to an in-memory current view for SecurityError/quota/read/write failures, allowing core binding/content initialization to continue. | Frontend contract and V-BROWSER blocked-storage scenario. | Deploy and repeat with hardened storage policies and private browsing. Memory fallback is intentionally non-persistent. |
| ENG-02 | **Remediated in branch** | Cloud mutations queue only network, 408, 425, 429, and 5xx failures; queue size/TTL/retries/backoff are bounded; entries are captured for and serialized to one user/key; logout clears them; account entries cannot become guest work. | Frontend product contracts; V-CONTRACT. | Monitor rejection/retry rates and expose support diagnostics if queue failures become user-visible. |
| ENG-03 | **Remediated in branch** | Generated category image markup is now the actual responsive visual consumer; runtime no longer fetches and discards the generated hero asset. | Generator `--check`, V-STATIC, and V-IMG. | Production network trace after Pages deployment. |
| ENG-04 | **Mitigated / pending activation** | Service-worker registration/update behavior is centralized; cache-clearing drift paths were removed. The built service worker derives its offline-cache identity from the complete deployable source graph, and `site-manifest.json` records the transformed asset graph instead of relying on an operator-edited cache counter. Edge compatibility protects already-open tabs from known prior fingerprint URLs without weakening immutable caching for current fingerprints. | Service-worker contracts; V-CONTRACT; V-SITE. | Some unused localization remains. Prove the derived identity and compatibility response on the deployed exact artifact; keep dead-code/key removal as measured maintenance rather than manual release-version coordination. |
| TEST-01 | **Remediated in branch** | Root [`package.json`](../package.json) pins Playwright. [`browser-matrix.mjs`](../scripts/browser-matrix.mjs) executes the full regression suite in Chromium, Firefox, and WebKit; release workflows require that matrix and the automated Axe/reflow run against the exact built artifact. Source contracts cover additional deterministic behavior. | V-CONTRACT; V-BROWSER passed 12/12; V-A11Y recorded 1,321 Axe rule passes against the exact artifact. | Release-commit CI must reproduce the matrix. WebKit is not physical Safari; retain physical-device and screen-reader acceptance for supported platforms. |
| TEST-02 | **Remediated in branch** | [`worker/package.json`](../worker/package.json) enforces 80% lines, 70% branches, and 85% functions with Node coverage; CI and API deploy validation run the thresholded suite. New account/security/route/privacy/scoring cases raise weak-route coverage. | V-WORKER: 164/164; 85.56/72.47/91.13. | Review thresholds as code grows; coverage percentage is not a substitute for risk-based cases. |
| TEST-03 | **Remediated in branch** | [`worker/scripts/local-integration.mjs`](../worker/scripts/local-integration.mjs) applies real local D1 migrations 0001–0008 and starts Wrangler/workerd with D1 and Durable Object bindings. It tests health, session, registration, consent, privacy requests, challenge cancellation, a real Battle WebSocket create/join/start/answer flow, seeded retention backlog, named failure, recovery, scheduled maintenance, logout, one-time recovery, and deletion. CI and deploy validation run it. | V-WORKER real integration pass. | Keep Wrangler pinned and repeat from the exact release commit. This is isolated local integration, not permission to run destructive checks against production accounts or D1. |
| TEST-04 | **Remediated in branch** | Scheduled jobs use `Promise.allSettled`, surface stable names for every failed job, and bounded cleanup probes identify persistent backlog without conflating exactly-full batches. The real local integration seeds a backlog beyond the processing ceiling, observes the named scheduled-job failure, drains it, and proves recovery on the next run. | [`worker/tests/index.test.mjs`](../worker/tests/index.test.mjs), [`worker/tests/db.test.mjs`](../worker/tests/db.test.mjs); V-WORKER. | Connect the same named failure/backlog evidence to production metrics/alerts after approved deployment. |

## Explicit residual, pending-activation, and owner-action register

The detailed table has exactly three **Residual** rows: `OPS-08`, `PRIV-03`, and `CNT-04`. The following facts must nevertheless remain open in release notes until independently completed; code-only mitigations are not production evidence.

1. **No production activation:** live behavior remains unquarantined: the live catalog exposes 3,553 cards, held datasets and held API leaderboard paths still return `200`, and the API remains on schema 6. The only external setup change is non-production D1 `jakh-recovery-drill` (`704609ac-4adc-411b-8a9c-894da397ee43`, EEUR); it is not proof of backup or restore.
2. **Recoverability:** no encrypted off-account export, named destination/key custodian, retention/RPO/RTO decision, checksum, isolated restore, or recovery receipt exists.
3. **Governance and credentials:** `main` protection has been prepared with pull-request-only updates, required `validate` and `Browser regression` checks, current-branch enforcement, conversation resolution, linear history, no force/delete, and no administrator bypass, but GitHub re-authentication must complete before it is saved and independently rechecked. Protected `production`/`recovery-drill` environments, least-privilege Cloudflare/GitHub secrets (including separate deploy/read/recovery credentials), and alert ownership remain unproved.
4. **Static edge activation:** exact static artifact/version rollback, headers, Brotli/cache policy, fingerprinted assets, and one-hop aliases exist in code and tests. The one-time `jakh-site` DNS cutover, live verification, and rollback exercise have not happened.
5. **DNS and mail (`OPS-08`):** the strict live gate fails for DNSSEC/DS, CAA, SPF `-all`, operator-supplied DKIM selector/placement, strict DMARC rejection/alignment, MTA-STS, and TLS-RPT. Routing, HTTPS, and IPv6 pass. Sender inventory and report-recipient ownership are unknown.
6. **Legal facts (`PRIV-03`):** operator/controller, postal address, country/governing law, privacy contact/complaint path, transfer basis, Terms, and minimum-age/parental-consent policy require truthful owner/legal input.
7. **Editorial closure (`CNT-01`, `CNT-04`, `CNT-05`):** four cards are legacy source-reviewed but 0/3,553 are evidence-complete; 0/278 high-stakes cards are complete and all 278 are quarantined from the runtime projection; 65 mutable candidates need disposition. The 382-packet queue is workflow evidence, not fact verification.
8. **Production operations:** synthetic-account credentials/ownership, issue/pager exercise, live Battle/cleanup checks, telemetry, and the cross-artifact production receipt remain unproved.
9. **Human acceptance:** the 12/12 three-engine matrix and 1,321 automated Axe passes do not replace physical Safari/iOS/Android, actual zoom, touch, screen-reader, authenticated workflow, and subjective visual review.
10. **Public source/history exposure:** runtime projection does not erase the five held datasets, their questions, or prior versions from a public GitHub repository. Repository visibility, a private content source, and any history-response plan require an explicit owner decision; history rewriting is not implied or authorized by this ledger.

## Ordered activation checklist

The sequence below is deliberately fail-closed. Stop at the first unmet gate and retain the last known-good state.

1. **Freeze and identify the candidate.** Record the exact merge commit, generated-file state, Worker version, expected schema 8, migrations 0007–0008, and all artifact checksums. Confirm the worktree is clean and reviewed.
2. **Close governance blockers before mutation.** Configure required branch checks, `production` and `recovery-drill` environments, required reviewers, and least-privilege secrets. Confirm repository issue alerts can be created. Do not infer these settings from workflow YAML.
3. **Resolve owner truth decisions.** Supply and legally approve the facts in `PRIV-03`. Keep the 278 high-stakes cards quarantined until qualified review is complete. Separately decide whether the public repository/history exposure is acceptable; do not rewrite history or change repository visibility implicitly.
4. **Run the exact-commit validation matrix.** Require V-REL, V-STATIC, V-SEO, V-CONTRACT (129/129), V-CONTENT structural/quarantine checks, V-IMG, V-GAME, V-WORKER (164/164), V-BROWSER (12/12), V-A11Y (1,321), V-PERF, and V-SITE (21/21) to pass. Preserve the expected strict editorial and V-DOMAIN failures as blockers to full closure; never lower them to manufacture a pass.
5. **Merge through protected `main`.** Build the exact 596-file public artifact from that commit, confirm 596/596 manifest/disk parity, and retain its build/source-graph/offline identities before any deployment.
6. **Deploy the guarded Pages containment baseline first.** While GitHub Pages remains authoritative for apex/`www`, deploy the exact 3,275-card projection through the protected `github-pages` environment. Require dual-origin receipts, all held routes/datasets to return `404`, public counts 51/3,275, and full monitoring. Never restore the pre-quarantine Pages tree as rollback.
7. **Run API `compatibility`.** Deploy the exact schema-tolerant Worker without applying a D1 migration or changing the bookmark. Require exact Worker identity on HTTP/WebSocket paths, schema-6 compatibility, 278-card API quarantine, monitor success, and proof D1 is unchanged. If the predecessor lacks version metadata, withhold automatic rollback rather than guessing.
8. **Establish recoverability before mutation.** Capture current Worker/D1 state and a Time Travel bookmark. Create an encrypted, checksummed, **off-account** D1 export; restore it into the isolated non-production database and retain the result. Until this and the same-commit compatibility receipt are mechanically enforceable, `migrate-final` must remain disabled.
9. **Observe, then run `migrate-final`.** Only the exact active compatibility Worker may authorize migrations 0007–0008. Migrate once, prove that the same release remains healthy on schema 8, and roll back only through the documented exact-version receipt path.
10. **Bootstrap and cut over the static Worker only after a safe baseline exists.** Provision separate least-privilege static deploy/read credentials, create `jakh-site`, capture DNS/HTTP snapshots, retain the safe Pages projection as rollback, and use a named operator/window. Freeze the exact API identity immediately before and after static deployment; verify headers, aliases, compression/cache policy, security.txt, offline behavior, and exact-version rollback.
11. **Verify accounts with a dedicated synthetic identity.** Exercise registration/recovery, session rotation, consent, privacy request/export, scoring/Battle, cleanup observation, logout, and deletion. Never use a real user account for destructive smoke tests.
12. **Verify discovery and external controls.** Crawl all 394 public sitemap URLs and verify held paths stay excluded. Separately stage and verify DNS/mail changes with mail-owner reports and rollback plans until V-DOMAIN strict passes.
13. **Run post-release and human acceptance.** Retain monitor, incident, backup/restore, release, browser, accessibility, performance, and crawl receipts. Complete physical-device/screen-reader/visual acceptance and the editorial queue before making the corresponding support, conformance, or factual-verification claim.

## Final acceptance criteria

### Minimum acceptance for the non-mutating containment rollout

- The exact release commit is on protected `main`, reviewed, and clean.
- All applicable source/build evidence keys V-REL through V-SITE are green for that exact commit; Worker thresholds remain at least 80% lines, 70% branches, and 85% functions. Any intentionally open strict content/domain gate is disclosed, not suppressed.
- Source state remains deterministic at 427 HTML files, 424 canonical/sitemap URLs, 56 categories, and 3,553 cards; deployable state is exactly 596 files, 397 HTML files, 394 sitemap URLs, 51 categories, and 3,275 cards.
- Required environment reviewers and least-privilege Pages/API compatibility secrets are independently confirmed; no unreviewed high-stakes content is represented as verified.
- Pages containment proves all held content is absent at apex and `www`; API compatibility proves all held API paths are denied and D1 remains unmodified on schema 6.
- Production monitoring and its incident issue route are exercised; rollback targets and receipts are retained.
- Static and API smoke tests pass from the retained manifest without weakening truthfulness: scores are server-checked/not proctored, fact review remains separate, offline scope remains bounded, and account/recovery limitations remain disclosed.
- No unresolved failure is hidden by retrying, suppressing, lowering thresholds, or editing generated output by hand.

Database migrations and static-Worker/DNS cutover have stricter acceptance: the encrypted off-account backup must have a verified checksum and successful isolated restore; the compatibility receipt must bind the same commit and active Worker; separate deploy/read/recovery credentials and protected environments must be proved; schema 8 must end healthy; and the safe Pages projection must remain the rollback target until exact static rollback is exercised.

### Acceptance required to call the original audit fully closed

The branch alone cannot satisfy this stronger standard. Full closure additionally requires:

- an exercised static rollback mechanism;
- verified production static security headers, Brotli/cache policy, and edge 301 aliases;
- approved DNS/mail hardening;
- truthful owner/legal entity, governing-law, Terms, transfer/complaint, and minors policy;
- source-backed, claim-mapped evidence completion for all 3,553 cards, with all 278 high-stakes cards independently qualified before any safety-sensitive verification claim;
- disposition and scheduled revalidation of all 65 mutable-language candidates;
- production field/lab evidence for the deployed two-split, language-sharded asset graph meeting agreed performance budgets;
- supported-browser, physical-device, screen-reader, OS high-contrast, actual zoom, touch, game, and authenticated-admin acceptance evidence;
- production proof that the deployed revisions and settings match this ledger.

Until those criteria are met, release language should say **“the implementation remediation plan is complete in the isolated branch, with documented residual and activation work”**, not “all audit findings are fixed” or “the website is 100% verified.”

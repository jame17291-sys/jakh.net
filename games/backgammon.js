(function () {
  'use strict';
  JakhGameFactory.registerRace({
    id: 'backgammon',
    track: 24,
    pieces: 8,
    die: 6,
    rules: 'Race every checker home. Choose which checker receives the current roll; first player to bear every checker off wins.',
  });
})();
